server:
  port: 8501

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8508/eureka/


spcloud:
  cassandra:
    contact:
      point: 127.0.0.1
    port : 9042
    keyspace:
      name: springcloud

    username : cassandra